import { IUserLogin } from "../../types/userTypes/IUserLogin";

export const auth = (data: IUserLogin) => {
    return () => {
        try {
            
        } catch (error) {
            console.log(error)
        }
    }
}