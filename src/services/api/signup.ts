import { IUserSignup } from "../../types/userTypes/IUserSignup"

export const signupUser = (data: IUserSignup) => {
    return () => {
        try {
            
        } catch (error) {
            console.log(error)
        }
    }
}